# {{PROJECT_NAME}}

Top-down canvas arcade game. Collect orbs, avoid drones.

- Move: Arrow keys / WASD, or drag anywhere on screen (virtual joystick)
- Restart: R, or tap after game over

`window.__foldForgeTest` exposes state and input hooks for the FOLD FORGE QA agent.
